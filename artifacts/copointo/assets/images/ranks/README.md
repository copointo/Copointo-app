# Rank Logos

Drop one PNG per rank here, named `rank-1.png` … `rank-10.png` (1 = lowest, 10 = highest).
The Levels screen will auto-pick them up via `data/rankLogos.ts`.
Until a file is added, the screen falls back to the emoji icon defined in `data/mockData.ts`.
